{
  "name": "websocketdemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --exec npm run server",
    "server": "npm-run-all --parallel server:*",
    "server:demo1": "PORT=8000 node server.js",
    "server:demo2": "PORT=8001 node server.js", 
    "server:demo3": "PORT=8002 node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5"
  }
}
